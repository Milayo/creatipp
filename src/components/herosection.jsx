import React from "react";
import HeaderComponent from "../components/header";

const HeroSection = () => {
  return (
    <div className="bg-landing-bg bg-cover h-screen">
      <HeaderComponent />
    </div>
  );
};

export default HeroSection;
